#include <iostream>
#include <vector>
using namespace std;

int main()
{
    // vector <int>  vec; //vectors and their type and name
    // vector <int> vec = {1 , 2 , 3};  //initializing a vector
    // vector <int> vec(3,0); //this initalizes a vector of size 3 and on each position it has a value of 0
    // cout<< vec[2];

    // for each loop in vector
    // where i is not an iterator but stores the value at each index after each iteration
    
    // for (int i : vec){
    //     cout<<i<<endl; //i can be named anything like val
    // }

    // vec.size();//size of vector

    // vec.push_back(25); //inserts at the end an element

    // vec.pop_back(); //delete the last element

    // vec.front(); //Prints the first value of vector
   
    // vec.back(); //Prints the last value of vector    

    // vec.at(i); //Returns a value at index i in a vector

    // vector <int> vec;

    // vec.push_back(1);
    // vec.push_back(2);
    // vec.push_back(3);
    // vec.push_back(4);
    // vec.push_back(5);

    // cout << vec.size()<<endl;
    // cout << vec.capacity()<<endl; //Dynamic MEmory ALlocation Topic stored in heap

    return 0;
}